# API base URL — the deployed crate.social API
VITE_API_URL=https://api.crate.social

# Base path for GitHub Pages.
# Set to /crate/ when deploying to brittanyellich.github.io/crate
# Leave as / (or unset) when deploying to a custom domain like crate.social
VITE_BASE_PATH=/crate-web/
